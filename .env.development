# =============================================================================
# AURORA Tax Classifier - Development Environment Configuration
# =============================================================================
# This file contains development-specific settings
# Safe to use for local development and testing
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
NODE_ENV=development
APP_NAME=AURORA Tax Classifier (Dev)
APP_VERSION=2.0.1

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# SQLite for development (easy setup, no external dependencies)
DATABASE_URL=sqlite:///./aurora.db

# PostgreSQL settings (if using docker-compose with PostgreSQL)
POSTGRES_USER=aurora
POSTGRES_PASSWORD=aurora-dev-password
POSTGRES_DB=aurora
POSTGRES_PORT=5432

# Uncomment to use PostgreSQL in development:
# DATABASE_URL=postgresql://aurora:aurora-dev-password@localhost:5432/aurora

# -----------------------------------------------------------------------------
# API AUTHENTICATION
# -----------------------------------------------------------------------------
# Development API Key (safe for local use)
API_KEY=aurora-dev-key-12345678

# Clerk Authentication (leave empty if not using)
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# EXTERNAL API KEYS
# -----------------------------------------------------------------------------
# Anthropic API (optional - add your key if testing AI features)
ANTHROPIC_API_KEY=

# Resend Email Service (optional - add your key if testing emails)
RESEND_API_KEY=
RESEND_FROM_EMAIL=dev@localhost

# -----------------------------------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------------------------------
# Local development URLs
BACKEND_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

FRONTEND_URL=http://localhost:3000
VITE_APP_URL=http://localhost:3000

# CORS Origins (allow both common dev ports)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# -----------------------------------------------------------------------------
# DOCKER PORT CONFIGURATION
# -----------------------------------------------------------------------------
# Default development ports
BACKEND_PORT=8000
FRONTEND_PORT=3000

# -----------------------------------------------------------------------------
# FILE STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
# Local storage in development
STORAGE_PATH=./storage

# Liberal file size for testing
MAX_FILE_SIZE_MB=10

# Allowed file extensions
ALLOWED_FILE_EXTENSIONS=.csv,.xlsx,.xls

# -----------------------------------------------------------------------------
# MODEL CONFIGURATION
# -----------------------------------------------------------------------------
MODEL_VERSION=baseline-v1.0
PREPROCESSING_VERSION=1.0
SCORING_VERSION=1.0

FISCAL_MODEL_PATH=models/koreksi_fiskal_lr.joblib
TAX_OBJECT_MODEL_PATH=models/objek_pph_lr.joblib

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------
# Verbose logging for development
LOG_LEVEL=DEBUG

# Human-readable format for development
LOG_FORMAT=text

# Log all requests for debugging
LOG_REQUESTS=true

# No log file in development (console only)
LOG_FILE_PATH=

# -----------------------------------------------------------------------------
# SECURITY SETTINGS
# -----------------------------------------------------------------------------
# No HTTPS in development
FORCE_HTTPS=false

# Simple secrets for development
SESSION_SECRET=dev-session-secret-not-for-production
CSRF_SECRET=dev-csrf-secret-not-for-production

# Lenient rate limiting for development
ENABLE_RATE_LIMIT=false
RATE_LIMIT_PER_MINUTE=1000

# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES (Vite)
# -----------------------------------------------------------------------------
VITE_APP_NAME=AURORA Tax Classifier (Dev)

# Enable debug mode
VITE_DEBUG_MODE=true

# Generous timeout for debugging
VITE_API_TIMEOUT=60000

# No analytics in development
VITE_ENABLE_ANALYTICS=false
VITE_GA_TRACKING_ID=

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION
# -----------------------------------------------------------------------------
EMAIL_TEMPLATE_DIR=./templates/email
ENABLE_EMAIL_NOTIFICATIONS=false
ADMIN_EMAIL=dev@localhost

# -----------------------------------------------------------------------------
# MONITORING & OBSERVABILITY
# -----------------------------------------------------------------------------
# No Sentry in development
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
# All features enabled in development
ENABLE_DIRECT_ANALYSIS=true
ENABLE_RISK_REPORTS=true
ENABLE_EXPLAINABILITY=true
ENABLE_JOB_HISTORY=true
MAX_HISTORY_JOBS=100

# -----------------------------------------------------------------------------
# BACKUP CONFIGURATION
# -----------------------------------------------------------------------------
# No automatic backups in development
ENABLE_AUTO_BACKUP=false
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS
# -----------------------------------------------------------------------------
# Enable hot reload for faster development
HOT_RELOAD=true

# Debug toolbar (if implemented)
DEBUG_TOOLBAR=true

# Seed database with test data
SEED_DATABASE=false

# -----------------------------------------------------------------------------
# NOTES FOR DEVELOPERS
# -----------------------------------------------------------------------------
# Quick Start:
# 1. Copy this file: cp .env.development .env
# 2. Adjust any settings as needed for your local environment
# 3. Run: make dev (or docker compose up)
# 4. Access: http://localhost:3000
#
# Database:
# - Using SQLite by default (no setup required)
# - To use PostgreSQL: uncomment DATABASE_URL above and run docker-compose
#
# API Keys:
# - Add your own API keys only if testing specific features
# - Never commit real API keys to version control
# =============================================================================
